/* Auto-generated by embed_data_segment.py — DO NOT EDIT.
 * The full, verified, byte-exact reconstruction of the
 * original's .data segment (28976 bytes) — see
 * PROGRESS.md Sec 5w for why this is embedded wholesale
 * rather than extracted tile-by-tile. Index directly with
 * the same absolute DS offsets the original assembly uses. */
#ifndef DS_POOL_H
#define DS_POOL_H

#include <stdint.h>
#include <stddef.h>

#define DS_POOL_SIZE 28976
extern const uint8_t ds_pool[DS_POOL_SIZE];

#endif

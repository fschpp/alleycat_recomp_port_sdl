/* Auto-generated by extract_walk_sprites.py — DO NOT EDIT.
 * 13 walk-cycle cat sprite frames, sliced from the verified
 * data segment using resolved DS pointer/dims tables (see
 * tools/resolve_data_segment.py). width_words is a WORD count
 * (verified against pointer-table deltas: real bytes/row =
 * width_words*2); height = scanlines. Pass width_words directly
 * to cga.c's blit_*() functions, which expect the same unit.
 * Definitions live in src/gen_cat_walk_frames.c (single shared
 * instance — extern here so pointer identity is consistent
 * across every translation unit that includes this header). */
#ifndef CAT_WALK_FRAMES_H
#define CAT_WALK_FRAMES_H

#include <stdint.h>
#include "../sprite.h"

extern const cat_walk_frame_t cat_walk_frames[13];

#endif
